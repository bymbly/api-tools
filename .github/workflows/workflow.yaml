name: CI

on:
  push:
    branches:
      - main
      - renovate/**
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.number || github.ref }}
  cancel-in-progress: true

jobs:
  prettier-format-check:
    name: Format Check
    uses: bymbly/gha-workflows/.github/workflows/prettier-format-check.yaml@67b82903f6ac226b2e596224b02103b812259f25 # main

  markdown-lint-check:
    name: Lint Check
    uses: bymbly/gha-workflows/.github/workflows/markdown-lint-check.yaml@67b82903f6ac226b2e596224b02103b812259f25 # main

  eslint-lint-check:
    name: ESLint Check
    uses: bymbly/gha-workflows/.github/workflows/eslint-lint-check.yaml@67b82903f6ac226b2e596224b02103b812259f25 # main

  npm-test:
    name: npm Test
    uses: bymbly/gha-workflows/.github/workflows/npm-test.yaml@67b82903f6ac226b2e596224b02103b812259f25 # main

  publish-npm-package:
    name: Publish NPM Package
    needs:
      [prettier-format-check, markdown-lint-check, eslint-lint-check, npm-test]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    permissions:
      contents: write
      packages: write
      issues: write
      pull-requests: write
      id-token: write # required for OIDC publishing
    uses: bymbly/gha-workflows/.github/workflows/npm-semantic-release.yaml@67b82903f6ac226b2e596224b02103b812259f25 # main
    with:
      registry-url: "https://registry.npmjs.org/"
